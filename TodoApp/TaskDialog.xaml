<Window x:Class="TodoApp.TaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoApp"
        xmlns:models="clr-namespace:TodoApp.Models"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Editar Tarea" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <ObjectDataProvider x:Key="PrioridadValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:Prioridad"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Description -->
        <TextBlock Text="Descripción:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="txtDescripcion" Grid.Row="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>

        <!-- Priority and Status -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <TextBlock Text="Prioridad:" FontWeight="Bold" Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbPrioridad" ItemsSource="{Binding Source={StaticResource PrioridadValues}}"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Bottom">
                <CheckBox x:Name="chkCompletada" Content="Completada"/>
            </StackPanel>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Cancelar" IsCancel="True" Click="btnCancel_Click" Width="80" Margin="0,0,10,0"/>
            <Button x:Name="btnOk" Content="Guardar" IsDefault="True" Click="btnOk_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
