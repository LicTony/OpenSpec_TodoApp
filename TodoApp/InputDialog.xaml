<Window x:Class="TodoApp.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="180" Width="350" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblMessage" Text="Message:" Margin="0,0,0,10" TextWrapping="Wrap"/>
        
        <TextBox x:Name="txtInput" Grid.Row="1" Margin="0,0,0,15" Padding="5"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="OK" IsDefault="True" Click="btnOk_Click" Width="70" Margin="0,0,10,0"/>
            <Button Content="Cancel" IsCancel="True" Click="btnCancel_Click" Width="70"/>
        </StackPanel>
    </Grid>
</Window>
